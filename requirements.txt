# Audio
pyaudio>=0.2.14
soundcard>=0.4.3
webrtcvad>=2.0.10
numpy>=1.26.0

# STT — Groq = cloud, faster-whisper = local offline fallback
groq>=0.9.0
faster-whisper>=1.0.3

# RAG
faiss-cpu>=1.8.0
sentence-transformers>=3.0.0

# OpenRouter / OpenAI-compatible SDK
openai>=1.35.0

# WebSocket server (used in Docker / WS_MODE=true)
websockets>=12.0

# Local dev convenience — loads .env automatically
python-dotenv>=1.0.0
