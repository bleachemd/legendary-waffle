# Audio
pyaudio>=0.2.14
soundcard>=0.4.3
webrtcvad>=2.0.10
numpy>=1.26.0

# STT — pick one or both (Groq = cloud, faster-whisper = local)
groq>=0.9.0
faster-whisper>=1.0.3

# RAG
faiss-cpu>=1.8.0
sentence-transformers>=3.0.0

# No extra http lib needed — we use urllib from stdlib
